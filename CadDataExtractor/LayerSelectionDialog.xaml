<Window x:Class="CadDataExtractor.LayerSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CAD数据提取设置" Height="550" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <!-- 使用Grid进行布局，可以很好地适应缩放 -->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 图层选择区域 -->
        <GroupBox Grid.Row="0" Header="选择要提取的图层" Padding="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListBox ItemsSource="{Binding Layers}" BorderThickness="1" BorderBrush="LightGray" Height="150">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="2"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,0">
                    <Button Content="全选" Command="{Binding SelectAllCommand}" Width="75" Margin="0,0,5,0"/>
                    <Button Content="清空" Command="{Binding ClearAllCommand}" Width="75"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- 输出设置 -->
        <GroupBox Grid.Row="1" Header="输出设置" Padding="5" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="输出CSV文件:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Grid.Column="1" Text="{Binding OutputPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                <Button Grid.Column="2" Content="浏览..." Command="{Binding BrowseCommand}" Width="75" Margin="5,0,0,0"/>
            </Grid>
        </GroupBox>

        <!-- 坐标系设置 -->
        <GroupBox Grid.Row="2" Header="坐标系设置" Padding="5" Margin="0,10,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <CheckBox x:Name="UseOriginCheckBox" Content="使用自定义原点" IsChecked="{Binding UseCustomOrigin, Mode=TwoWay}"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,5,0,5">
                    <TextBlock Text="原点X:" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding OriginX, Mode=TwoWay}" Width="80" Margin="5,0,10,0" IsEnabled="{Binding IsChecked, ElementName=UseOriginCheckBox}"/>
                    <TextBlock Text="原点Y:" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding OriginY, Mode=TwoWay}" Width="80" Margin="5,0,0,0" IsEnabled="{Binding IsChecked, ElementName=UseOriginCheckBox}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="单位缩放:" VerticalAlignment="Center"/>
                    <ComboBox ItemsSource="{Binding UnitScaleOptions}" SelectedIndex="{Binding SelectedUnitScaleIndex, Mode=TwoWay}" Width="120" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- 确定/取消按钮 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="80" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="取消" Width="80" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
